language: python
python:
  - "3.5"
# command to install dependencies
install: "pip install -r requirements/test.txt"
# command to run tests
script: ./manage.py test --settings=fullystacked.settings.test
